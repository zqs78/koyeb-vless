name: vless-service
services:
- name: vless
  ports:
  - port: 33333
    protocol: HTTP
  scaler:
    min: 1
    max: 2
  healthcheck:
    path: /
    port: 8000  # 修正为正确的8000端口
    initial_delay: 30
    period: 10
    timeout: 5
