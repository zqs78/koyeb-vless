name: vless-service
services:
- name: vless
  ports:
  - port: 33333
    protocol: HTTP
  scaler:
    min: 1
    max: 2
  healthcheck:
    path: /
    port: 8080  # 改为健康检查服务的端口
    initial_delay: 30
    period: 10
    timeout: 5
